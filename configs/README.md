# Configurations

Placeholder for documented experiment configurations covering datasets, edge inference, cloud routing, adaptation, and evaluation. Configuration names and schemas will be defined with the implementation.

Use repository-relative paths in shared examples. Keep credentials and deployment-specific endpoints outside tracked files. Local configuration files named `local.*` are ignored by the supplied `.gitignore`.
