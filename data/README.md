# Local data

Dataset contents are not included. See [dataset documentation](../docs/datasets.md) for manuscript split counts and pending access details. The supplied `.gitignore` excludes local contents of this folder except this README.
